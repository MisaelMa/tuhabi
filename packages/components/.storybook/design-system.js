import { create } from '@storybook/theming/create';

import logo from '../../../packages/commons/assets/logo_tudn.png';

export default create({
  base: 'light',
  brandTitle: 'TUDN Design System',
  brandUrl:'',
  brandImage: logo,
});
